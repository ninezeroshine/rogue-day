# 🛠️ Локальная разработка

Инструкция по настройке локального окружения для разработки без Telegram аутентификации.

## 📋 Требования

- Python 3.11+
- PostgreSQL (локально или через Docker)
- Node.js 18+ (для фронтенда)

## 🔧 Настройка бэкенда

### 1. Установите зависимости

```bash
cd backend
pip install -e ".[dev]"
```

### 2. Настройте переменные окружения

Создайте файл `backend/.env`:

```env
# Database (use SQLite for easier local dev, or PostgreSQL)
# Option 1: SQLite (easiest, no setup needed)
DATABASE_URL=sqlite+aiosqlite:///./rogue_day.db

# Option 2: PostgreSQL (if you have it installed)
# DATABASE_URL=postgresql://user:password@localhost:5432/rogue_day

# Telegram (можно оставить пустым для dev mode)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Dev Mode - ВКЛЮЧИТЕ для локальной разработки
ALLOW_DEV_MODE=true
DEV_TELEGRAM_ID=123456789

# Security (для dev можно оставить дефолтное значение)
SECRET_KEY=dev-secret-key-change-in-production

# CORS (уже включает localhost)
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
```

### 3. Создайте dev пользователя

```bash
# Запустите скрипт для создания dev пользователя
python backend/scripts/create_dev_user.py
```

Или создайте пользователя вручную через API после запуска сервера.

### 4. Запустите бэкенд

```bash
cd backend
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

Бэкенд будет доступен на `http://localhost:8000`

## 🎨 Настройка фронтенда

### 1. Установите зависимости

```bash
cd app
npm install
```

### 2. Настройте переменные окружения

Создайте файл `app/.env.local`:

```env
# Укажите локальный URL бэкенда
VITE_API_URL=http://localhost:8000
```

### 3. Запустите фронтенд

```bash
cd app
npm run dev
```

Фронтенд будет доступен на `http://localhost:5173`

## ✅ Проверка работы

1. Откройте `http://localhost:5173` в браузере
2. Приложение должно автоматически использовать dev режим
3. Все API запросы будут идти на локальный бэкенд
4. Аутентификация будет обходиться через `DEV_TELEGRAM_ID`

## 🔒 Безопасность

⚠️ **ВАЖНО:** 
- `ALLOW_DEV_MODE=true` должен быть **ТОЛЬКО** в локальном `.env`
- **НИКОГДА** не коммитьте `.env` файлы с `ALLOW_DEV_MODE=true` в Git
- На Railway/production этот флаг должен быть `false` или отсутствовать

## 🐛 Troubleshooting

### Ошибка "User not found"

Запустите скрипт создания dev пользователя:
```bash
python backend/scripts/create_dev_user.py
```

### Ошибка подключения к БД

Убедитесь, что PostgreSQL запущен и `DATABASE_URL` правильный:
```bash
# Проверка подключения
psql $DATABASE_URL
```

### CORS ошибки

Убедитесь, что в `CORS_ORIGINS` указан правильный URL фронтенда (обычно `http://localhost:5173`)

### API запросы идут на production

Проверьте, что в `app/.env.local` указан `VITE_API_URL=http://localhost:8000`

## 📝 Примечания

- Dev режим автоматически создает пользователя при первом запросе, если его нет
- Все данные сохраняются в локальную БД
- Для тестирования можно использовать разные `DEV_TELEGRAM_ID` для разных пользователей

